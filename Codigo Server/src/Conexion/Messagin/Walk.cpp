#include "Walk.h"
#include <stdio.h>
#include <string.h>

Walk::Walk()
{
	header=CLIENT_WALK;
}

string Walk::action(char* mensaje,string identifier)
{
	string* cadena = new string();

	char heading=mensaje[1];


	return (*cadena);
}
